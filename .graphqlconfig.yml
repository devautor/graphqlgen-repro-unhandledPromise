# For graphql-cli (not being used in this project)
projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  prisma:
    schemaPath: src/generated/prisma-client-ts/prisma-schema.ts
    extensions:
      prisma: prisma/prisma.yml
